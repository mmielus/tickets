<h1> Wszystkie wydarzenia</h1>
<ul>
  <% for event in @events %>
    <li><%= link_to event.artist, "/events/#{event.id}" %></li>
  <% end %>
</ul>
<% if user_signed_in? %>
  <%= link_to 'Nowe ogłoszenie', events_new_path %>
<% end %>

<%= link_to 'Cofnij', "/" %>
